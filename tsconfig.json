{
    "compilerOptions": {
      "target": "es6",                   // Define a versão do JavaScript para a saída
      "module": "commonjs",               // Define o sistema de módulos
      "strict": true,                     // Ativa todas as verificações rigorosas
      "esModuleInterop": true,            // Garante a interoperabilidade com módulos ES
      "skipLibCheck": true,               // Ignora verificação de tipos de bibliotecas para acelerar a compilação
      "forceConsistentCasingInFileNames": true, // Garante que os nomes de arquivos sejam tratados com diferenciação consistente de maiúsculas/minúsculas
      "outDir": "./dist",                 // Define a pasta de saída como "dist"
      "rootDir": "./src"                  // Define a pasta de entrada como "src"
    },
    "include": ["src/**/*.ts"],           // Inclui todos os arquivos TypeScript na pasta src
    "exclude": ["node_modules"]           // Exclui a pasta node_modules
  }
  